# Assistant-UI React AG-UI Patch Inventory

Source patch: `patches/@assistant-ui__react-ag-ui@0.0.26.patch`

Current npm latest checked during execution: `@assistant-ui/react-ag-ui@0.0.31`.

## Classification

| Patch area                                                                                     | Classification           | Reason                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `historyLoadKey`, `_loadKey`, `loadGeneration`, stale-load guards                              | Upstream                 | Generic async history loading correctness. The runtime should ignore stale history loads and allow callers to force reloads without app-specific code.                                                     |
| `waitForInitialLoad()` before append/reload/resume/startRun                                    | Upstream                 | Generic runtime ordering guarantee. Appends and resumes should not race initial history hydration.                                                                                                         |
| `externalMessagesStrategy: "merge-after-local-mutations"`                                      | Keep temporarily         | Generic reconciliation shape, but the current need is driven by Terragon optimistic append plus durable replay. Revisit after transcript/submission ownership is simplified.                               |
| `hasLocalRuntimeMutations`, `mergeExternalMessages`, `dedupeMessages`, assistant merge scoring | Keep temporarily         | Reconciliation behavior prevents local optimistic messages from being replaced by replay/hydration. It may move into app projection later, but removing it now risks duplicate or missing transcript rows. |
| `startRun(..., intent)` / `withRunIntent()` package-level Terragon intent injection            | Closed                   | No matching hunk remains in the current patch. Product metadata is repo-owned via `apps/www/src/lib/run-metadata.ts`, encoded by `routeComposerSubmit()`, and decoded by `dispatchFollowUpFromAppend()`.   |
| Resume calls passing package-level `intent: "resume"`                                          | Closed for package patch | No matching hunk remains in the current patch. Resume/append classification is app-owned via `forwardedProps.runConfig.terragon.intent` and `classifyAgUiPostIntent()`.                                    |
| Target assistant message support from AG-UI `messageId` / `parentMessageId`                    | Upstream                 | Raw AG-UI events carry target identity. The runtime aggregator should update the intended assistant message instead of always creating a placeholder.                                                      |
| Synthetic assistant creation and `removeEmptySyntheticAssistant()`                             | Upstream                 | Generic cleanup required by target-message routing to avoid empty placeholder messages.                                                                                                                    |
| Tool-call parent resolution and target emission                                                | Upstream                 | Generic AG-UI event fidelity. Tool args/results should update the assistant message implied by event identity.                                                                                             |
| `UseAgUiRuntimeOptions.historyLoadKey`                                                         | Upstream                 | Public runtime option for forcing history reloads.                                                                                                                                                         |
| `UseAgUiRuntimeOptions.externalMessagesStrategy`                                               | Keep temporarily         | Potential upstream option, but the exact strategy name may be too app-shaped. Revisit after local projection ownership is decided.                                                                         |

## Execution Notes

- No hunk should remain because it mentions Terragon product semantics.
- The package-level `custom.terragon.intent` deletion target is closed in the current patch; keep Terragon run metadata in repo-owned modules.
- The most likely long-lived hunks are generic runtime mechanics: history load keys, load ordering, target message routing, and synthetic assistant cleanup.
- Before upgrading from `0.0.26` to `0.0.31`, compare upstream source for the generic hunks rather than assuming the patch still applies cleanly.

## Closeout Ledger

| Patch area                                                                                 | Replacement owner                                                                                                                                                                                                               | Characterization tests                                                                                                                                                                                                | Remaining route invariant                                                                                                                                                                             | Rollback condition                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `historyLoadKey`, `_loadKey`, `loadGeneration`, stale-load guards                          | Upstream `@assistant-ui/react-ag-ui` runtime; app-owned key derivation remains in `apps/www/src/components/chat/assistant-ui/runtime-resume-policy.ts`.                                                                         | `apps/www/src/components/chat/assistant-ui/runtime-resume-policy.test.ts`, `apps/www/src/components/chat/assistant-ui/assistant-runtime-session.test.tsx`.                                                            | `historyLoadKey` must change when active/idle/retry history changes so stale loads cannot overwrite the current thread runtime.                                                                       | Remove this patch only after upstream exposes equivalent keyed history reload and stale-load cancellation, then keep the app key tests green against the unpatched package.                                  |
| `waitForInitialLoad()` before append/reload/resume/startRun                                | Upstream `@assistant-ui/react-ag-ui` runtime.                                                                                                                                                                                   | `apps/www/src/components/chat/assistant-ui/assistant-runtime-session.test.tsx`, `apps/www/src/components/chat/assistant-ui/native-thread.test.tsx`.                                                                   | Appends, resumes, reloads, and run starts must not race the initial durable history load for the same thread.                                                                                         | Remove this patch only after upstream awaits initial history before every mutation path and the native runtime tests still prove no pre-hydration append loss.                                               |
| `externalMessagesStrategy: "merge-after-local-mutations"` and local mutation merge scoring | Temporary package patch plus app-owned runtime configuration in `apps/www/src/components/chat/assistant-ui/assistant-runtime-session.tsx`; durable transcript ownership is repo-owned through AG-UI POST and server projection. | `apps/www/src/components/chat/assistant-ui/native-thread.test.tsx`, `apps/www/src/components/chat/assistant-ui/assistant-runtime-session.test.tsx`, `apps/www/src/components/chat/thread-view-model/reducer.test.ts`. | assistant-ui is the only rendered transcript interpreter; `ThreadViewModel` must keep `messages: []` and expose durable state via `dbMessages`, `sidePanel`, lifecycle, artifacts, and meta sidecars. | Remove this patch when local optimistic transcript mutations are fully replaced by durable AG-UI replay/queue state and the runtime can safely use upstream `replace` without duplicate or missing messages. |
| Target assistant message support from AG-UI `messageId` / `parentMessageId`                | Upstream `@assistant-ui/react-ag-ui` `RunAggregator`; Terragon event fidelity is app-owned in `apps/www/src/components/chat/terragon-run-aggregator.ts` and server event projection.                                            | `apps/www/src/components/chat/terragon-run-aggregator.test.ts`, `apps/www/src/app/api/ag-ui/[threadId]/route.test.ts`, `apps/www/src/server-lib/ag-ui-side-effect-messages.test.ts`.                                  | Text, reasoning, tool calls, and tool results must update the assistant message identified by AG-UI event identity rather than creating unrelated placeholders.                                       | Remove this patch only after upstream preserves target message identity for text and tool streams and the Terragon aggregator tests match upstream behavior.                                                 |
| Synthetic assistant creation and `removeEmptySyntheticAssistant()`                         | Upstream `@assistant-ui/react-ag-ui` runtime.                                                                                                                                                                                   | `apps/www/src/components/chat/ag-ui-messages-reducer.test.ts`, `apps/www/src/components/chat/assistant-ui/native-thread.test.tsx`.                                                                                    | Placeholder assistant messages may exist only as transient runtime scaffolding and must be removed when a targeted assistant message receives the real content.                                       | Remove this patch when upstream no longer leaves empty synthetic assistant messages after targeted event streams.                                                                                            |
| Tool-call parent resolution and target emission                                            | Upstream `@assistant-ui/react-ag-ui` `RunAggregator`; app-side compatibility stays in `apps/www/src/components/chat/terragon-run-aggregator.ts`.                                                                                | `apps/www/src/components/chat/terragon-run-aggregator.test.ts`, `apps/www/src/server-lib/ag-ui-side-effect-messages.test.ts`, `packages/shared/src/model/agent-event-log.test.ts`.                                    | Tool args/results must stay attached to the same assistant message as their parent text/tool stream, including nested parent-tool references.                                                         | Remove this patch when upstream resolves tool parent identity without app-specific patching and the event-log plus UI aggregator tests stay green.                                                           |
| Package-level Terragon run intent injection                                                | `apps/www/src/lib/run-metadata.ts`, `apps/www/src/components/promptbox/composer-submit-routing.ts`, and `apps/www/src/server-lib/ag-ui/ag-ui-command-handler.ts`.                                                               | `apps/www/src/components/promptbox/composer-submit-routing.test.ts`, `apps/www/src/server-lib/ag-ui/ag-ui-command-handler.test.ts`, `apps/www/src/app/api/ag-ui/[threadId]/route.test.ts`.                            | Product intent must travel through repo-owned run metadata, not patched package internals.                                                                                                            | Closed now; if a future patch reintroduces Terragon-specific package metadata, reject it and route through repo-owned run metadata instead.                                                                  |
