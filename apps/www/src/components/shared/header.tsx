export const headerClassName = "h-12";

// Matches the inset content surface (bg-card) the app renders on, so the
// header reads as part of the card; a hairline divider does the elevation work.
export const headerSurfaceClassName = "border-b border-hairline bg-card";
